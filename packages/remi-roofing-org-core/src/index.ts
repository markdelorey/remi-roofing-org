/**
 * Example function.
 *
 * @example
 *
 * Use from other apps/packages:
 *
 * ```ts
 * import { hello } from '@repo/remi-roofing-org-core'
 *
 * hello()
 * ```
 */
export function hello() {
	return 'Hello, world!'
}
